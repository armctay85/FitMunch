/**
 * DISABLED — published flat SVG + raw URL captions (not clickable on IG).
 * Use: node scripts/make-ig-carousel.mjs && node scripts/publish-ig-gated.mjs
 */
console.error('BLOCKED: post-ig-4x5.mjs retired. Use publish-ig-gated.mjs after quality gate.');
process.exit(1);
