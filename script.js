/**
 * FitMunch client logic lives in `public/script.js` (that is what Express serves).
 * Tests and any Node-based tooling load this file, which re-exports the same implementation.
 */
module.exports = require('./public/script.js');
